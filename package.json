{
  "name": "s-seer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd backend && node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "12.x"
  }
}
